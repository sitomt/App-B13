# Dependencias y build
node_modules
dist

# Variables de entorno (credenciales)
.env
*.local

# macOS
.DS_Store

# Skills instaladas (binarios locales, no parte del código fuente)
.agents/

# Planes y memoria de sesión de Claude Code
.claude/

# Vercel (generado por CLI/plugin al hacer deploy)
.vercel/
